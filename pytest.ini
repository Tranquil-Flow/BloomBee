[pytest]
# Do not declare pytest-timeout options unless the pytest-timeout plugin is in
# the test environment. Unknown options produce noisy PytestConfigWarning and
# do not enforce a timeout. Re-enable a suite-wide timeout only with an explicit
# dependency on pytest-timeout or an equivalent local plugin.
